blog_module.controller('BlogController', ['$scope', '$window', 'toastr', 'AppUtil',
    BlogController]);

function BlogController($scope, $window, toastr, AppUtil) {

    init();

    function init() {
    }
}
